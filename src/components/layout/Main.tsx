import { ReactElement } from 'react';
import HeroSection from '../sections/HeroSection';

const Main = (): ReactElement => {
  return (
    <main className=''>
      <HeroSection />
    </main>
  );
};

export default Main;
