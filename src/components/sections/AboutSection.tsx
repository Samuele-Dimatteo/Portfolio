import Text from '@/components/common/Text';

const AboutSection = () => {
  return (
    <section className="flex justify-between h-screen">
      <Text textElement={'span'} size='xl' weight='bd' color='foreground'>This is the About Section</Text>
    </section>
  );
};

export default AboutSection;
